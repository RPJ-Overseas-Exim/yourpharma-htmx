package ui

import "github.com/RPJ-Overseas-Exim/yourpharma-htmx/globs"

templ ButtonDiv(class string){
    <div class={
        globs.Merger.Merge(`flex items-center justify-center rounded-md font-semibold ` + class)
        }>
        { children...}
    </div>
}
